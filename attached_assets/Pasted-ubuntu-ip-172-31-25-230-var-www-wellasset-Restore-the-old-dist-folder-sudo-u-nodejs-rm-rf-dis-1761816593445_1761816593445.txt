ubuntu@ip-172-31-25-230:/var/www/wellasset$ # Restore the old dist folder
sudo -u nodejs rm -rf dist
sudo -u nodejs mv dist.old dist

# Check if server/index.ts has my updated code (should NOT have top-level vite import)
head -15 /var/www/wellasset/server/index.ts

# If it has my changes, rebuild just the server part
sudo -u nodejs npx esbuild server/index.ts --platform=node --packages=external --bundle --format=esm --outdir=dist

# Kill PM2 and restart
sudo -u nodejs /home/nodejs/node_modules/.bin/pm2 kill
sudo -u nodejs bash -c "cd /var/www/wellasset && source .env.production && /home/nodejs/node_modules/.bin/pm2 start ecosystem.config.cjs --env production"

# Test
sleep 5
curl http://localhost:5000/health
import express, { type Request, Response, NextFunction } from "express";
import { registerRoutes } from "./routes";
import { setupVite, serveStatic, log } from "./vite";
import path from "path";

const app = express();

declare module 'http' {
  interface IncomingMessage {
    rawBody: unknown
  }
}
app.use(express.json({
  verify: (req, _res, buf) => {
    req.rawBody = buf;
✘ [ERROR] Could not resolve "../vite.config"

    server/vite.ts:6:23:
      6 │ import viteConfig from "../vite.config";
        ╵                        ~~~~~~~~~~~~~~~~

1 error
[PM2] Applying action deleteProcessId on app [all](ids: [ 0 ])
[PM2] [all](0) ✓
[PM2] [v] All Applications Stopped
[PM2] [v] PM2 Daemon Stopped
[PM2] Spawning PM2 daemon with pm2_home=/home/nodejs/.pm2
[PM2] PM2 Successfully daemonized
[PM2][WARN] Applications wellasset-app not running, starting...
[PM2][WARN] Environment [production] is not defined in process file
[PM2][WARN] App wellasset-app has option 'wait_ready' set, waiting for app to be ready...
[PM2] App [wellasset-app] launched (1 instances)
┌────┬────────────────────┬──────────┬──────┬───────────┬──────────┬──────────┐
│ id │ name               │ mode     │ ↺    │ status    │ cpu      │ memory   │
├────┼────────────────────┼──────────┼──────┼───────────┼──────────┼──────────┤
│ 0  │ wellasset-app      │ cluster  │ 2    │  │ 0%       │ 0b       │
└────┴────────────────────┴──────────┴──────┴───────────┴──────────┴──────────┘
curl: (7) Failed to connect to localhost port 5000 after 0 ms: Couldn't connect to server
ubuntu@ip-172-31-25-230:/var/www/wellasset$